{
  "name": "backstop-retry-failed-scenarios",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "jest",
    "test-watch": "jest --watch",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gts": "^2.0.2",
    "jest": "^26.0.1",
    "test-fixture": "^2.4.1",
    "typescript": "^3.8.3",
    "@types/node": "^13.11.1"
  },
  "dependencies": {
    "command-line-args": "^5.1.1",
    "command-line-usage": "^6.1.0",
    "escape-string-regexp": "^4.0.0",
    "xml2json": "^0.12.0"
  },
  "bin": {
    "backstop-retry-failed-scenarios": "cli.js"
  }
}
